 void setup() {
 size(1000, 600);
 colorMode(HSB, 360, 100, 100); 
 for (int h = 0; h < 360; h++) {
 stroke(h, 100, 100);
 line(h, 0, h, 100);
 }
 
 void mousePressed() {
 color c = get(mouseX, mouseY);//gets you the color of the pixel;
 stroke(c);
 fill(c);
 rect(5, 410, 40, 40);
 }
